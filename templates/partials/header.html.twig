<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="{{ path('homepage') }}">Symbnb</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('homepage') }}">Acceuil</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('ads_index') }}">Annonces</a>
            </li>
        </ul>
        <ul class='navbar-nav ml-auto'>
            {% set a = app.user %}
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
                        <img src="{{ a.picture }}" class="avatar avatar-mini" alt="Avatar de {{ a.fullname }}">
                        {{ a.fullname }}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path("ads_create") }}" class="dropdown-item">Crée une annonce</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path("account_user") }}" class="dropdown-item">Mon compte</a>
                        <a href="{{ path("password_update_account") }}" class="dropdown-item">Modifier mon profil</a>
                        <div class="dropdown-divider"></div>
                            <a href="{{ path('logout_account') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                 <li class="nav-item">
                    <a href="{{ path("register_account") }}" class="nav-link">Register</a>
                </li> 
                <li class="nav-item">
                    <a href="{{ path("login_account") }}" class="nav-link">Login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
{% for label,messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{label}}">
            {% for message in messages %}
                <p>{{ message }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}