<nav class="navbar navbar-expand-lg navbar-dark bg-info">
        <a class="navbar-brand" href="{{ path('homepage') }}">Symbnb</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('admin_dashboard') }}"><i class="fi-xnsuxl-bar-chart-solid"></i> Dashboard</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('admin_ads_index') }}"><i class="fi-xnsuxl-house-solid"></i> Annonces</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('admin_bookings_index') }}"><i class="fi-xwsuxl-folder-solid"></i> Réservations</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="{{path('admin_comments_index')}}"><i class="fi-xnsuxl-comment-square-dot-solid"></i> Commentaires</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="{{path('admin_users_index')}}"><i class="fi-xnsuxl-team-solid"></i> Utilisateurs</a>
            </li>
        </ul>
        <ul class='navbar-nav ml-auto align-items-center'>
            {% set a = app.user %}
            <li class="nav-item">
                <a href="{{ path('homepage')}}" target="blank" class="nav-link"><i class="fi-xnsuxl-eye-solid"></i> Voir le site</a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
                        <img src="{{ a.picture }}" class="avatar avatar-mini" alt="Avatar de {{ a.fullname }}">
                        {{ a.fullname }}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
{% for label,messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{label}}">
            {% for message in messages %}
                <p>{{ message }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}