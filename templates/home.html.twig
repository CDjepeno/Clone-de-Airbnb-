{% extends "base.html.twig" %}

{% block title %}Bienvenue sur notre réseau{% endblock %}
{% block header %} <h2>Voici le header</h2> {% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron mt-4">
        <h1 class="display-3">Bienvenue sur SymBNB </h1>
        <p class="lead">Nous mettons en relations les voyageurs et les propriétaire qui veulent profiter d'opportunités et gagner de l'argent en proposant leur bien à la location  </p>
        <hr class="my-4">
        <p>Vous êtes voyageurs ? Et vous voulez voir où vous pourrez loger pour votre prochain déplacement ?</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="{{ path('ads_index') }}" role="button">Voir nos annonces</a>
        </p>
    </div>
    <pre>
    </pre>
    <h2 class=" my-4">Nos appartements phars !</h2>
    <div class="row">
      {% for data in ads %}
          {% include 'ad/_ad.html.twig' with {'ad': data.annonce} %}
      {% endfor %}
    </div>
    <h2 class=" my-4">Nos propriétaire stars !</h2>
    <div class="row">
        {% for user in users %}
        {% set user = user.user %}
            <div class="col">
                <div class="card border-secondary mb-3">
                    <div class="card-body">
                        <h4>{{user.fullName}}</h4>

                        <hr style="colors:white;">

                        <div class="card-text">
                            <img src="{{user.picture}}" alt="Avatar de kevin" class="float-left mr-3">
                            <p>{{user.introduction}}</p>
                            <a href="{{path('user_show', {'slug': user.slug}) }}" class="btn btn-primary float-right">En savoir plus !</a>
                        </div>
                    </div>
                </div>
            </div>    
        {% endfor %}
    </div>
</div>
{% endblock %}

